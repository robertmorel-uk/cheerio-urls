create database bisscryptoapi;
use bisscryptoapi;

urlID, urlTitle, urlLink, urlDescription, urlSource, urlDate

CREATE TABLE bisscryptoapientries (
    urlID VARCHAR(512), 
    urlTitle VARCHAR(512),
    urlLink VARCHAR(512),
    urlDescription VARCHAR(1024), 
    urlSource VARCHAR(512),
    urlDate VARCHAR(128)
    );